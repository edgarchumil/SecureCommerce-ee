import { FormEvent, useState } from 'react'
import { Link } from 'react-router-dom'
import { requestPasswordReset } from '../api/administration'
export function PasswordResetPage(){const[message,setMessage]=useState('');const submit=async(event:FormEvent<HTMLFormElement>)=>{event.preventDefault();const data=new FormData(event.currentTarget);setMessage((await requestPasswordReset(String(data.get('email')))).message)};return <main className="grid min-h-screen place-items-center bg-slate-100 p-5"><form onSubmit={submit} className="w-full max-w-md rounded-xl border bg-white p-6"><h1 className="text-2xl font-bold text-blue-950">Recuperar contraseña</h1><p className="mt-2 text-slate-600">Enviaremos instrucciones si la cuenta existe.</p><label className="mt-5 block">Correo<input name="email" type="email" required className="mt-1 w-full rounded-lg border p-2"/></label><button className="mt-4 w-full rounded-lg bg-blue-950 p-2 text-white">Solicitar instrucciones</button>{message&&<p role="status" className="mt-4">{message}</p>}<Link className="mt-4 block text-blue-700" to="/login">Volver al acceso</Link></form></main>}
